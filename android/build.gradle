apply plugin: 'com.android.library'

android {
    compileSdkVersion 33              // Updated to a more recent SDK version
    buildToolsVersion "33.0.0"

    defaultConfig {
        minSdkVersion 21              // Updated to a more recent minimum SDK version
        targetSdkVersion 33           // Updated to match compile SDK
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation 'com.google.zxing:core:3.2.1'  // Replaced 'compile' with 'implementation'
    implementation "com.facebook.react:react-native:0.71.0"  // Specify RN version instead of '+'
}
